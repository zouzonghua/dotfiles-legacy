# Clipboard synchronization
# bind -Tcopy-mode C-w               send -X copy-pipe-and-cancel 'pbcopy' \; display-message "copied to system clipboard"
# bind -Tcopy-mode M-w               send -X copy-pipe-and-cancel 'pbcopy' \; display-message "copied to system clipboard"
# bind -Tcopy-mode MouseDragEnd1Pane send -X copy-pipe-and-cancel 'pbcopy' \; display-message "copied to system clipboard"

# 为 vi 模式增加拖拽复制支持
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-pipe-and-cancel "pbcopy" \; display-message "copied to system clipboard"

# 如果你还需要 vi 模式下的常用快捷键（如 y）也支持同步剪贴板
bind -T copy-mode-vi y send -X copy-pipe-and-cancel "pbcopy" \; display-message "copied to system clipboard"

# Watch macOS appearance changes and refresh tmux theme
run-shell -b ~/.config/tmux/scripts/theme_watch.sh

# if-shell "uname -s | grep -q Darwin" {
#     # --- 默认：Light Mode (浅色模式) ---
#     set-option -g status-bg "colour255"
#     set-option -g status-fg "colour239"
#
#     # 浅色模式颜色方案：[正常:深灰, 警告:橙色, 高压:深红]
#     set-option -g status-right "#[fg=colour239,bg=colour255] #(~/.config/tmux/mem_status.sh 'colour239' 'colour208' 'colour160')  #(~/.config/tmux/cpu_load.sh 'colour239' 'colour208' 'colour160') #[default]"
#     set-option -g status-left "#[fg=colour239,bg=colour255]#{?client_prefix,#[reverse],} Session: #S #[default]"
#
#     # --- 自动切换：Dark Mode (深色模式) ---
#     if-shell "defaults read -g AppleInterfaceStyle | grep -q Dark" {
#         set-option -g status-bg "colour238"
#         set-option -g status-fg "colour250"
#
#         # 深色模式颜色方案：[正常:浅绿, 警告:黄色, 高压:红色] (保持你之前的喜好)
#         set-option -g status-right "#[fg=colour250,bg=colour238] #(~/.config/tmux/mem_status.sh 'colour121' 'colour220' 'colour197')  #(~/.config/tmux/cpu_load.sh 'colour121' 'colour220' 'colour197') #[default]"
#         set-option -g status-left "#[fg=colour255,bg=colour238]#{?client_prefix,#[reverse],} Session: #S #[default]"
#     }
# }

# --- ~/.config/tmux/macos.conf ---

if-shell "defaults read -g AppleInterfaceStyle 2>/dev/null | grep -q Dark" {
    # --- 深色模式 (Dark) ---
    # 背景使用深灰 (238)，文字使用浅灰 (250)
    set-option -g status-bg "colour238"
    set-option -g status-fg "colour250"

    # 颜色参数：正常:76(绿), 警告:142(黄), 高压:160(红)
    set-option -g status-right "#[fg=colour250,bg=colour238] #(~/.config/tmux/scripts/mem_status.sh '76' '142' '160')   #(~/.config/tmux/scripts/cpu_load.sh '76' '142' '160') #[default]"
    set-option -g status-left "#[fg=colour255,bg=colour238]#{?client_prefix,#[reverse],} Session: #S #[default]"
} {
    # --- 浅色模式 (Light) ---
    # 背景使用近白色 (255)，文字使用深色 (239)
    set-option -g status-bg "colour255"
    set-option -g status-fg "colour239"

    # 颜色参数：正常:34(深绿), 警告:100(黄褐), 高压:124(深红)
    set-option -g status-right "#[fg=colour239,bg=colour255] #(~/.config/tmux/scripts/mem_status.sh '34' '100' '124')   #(~/.config/tmux/scripts/cpu_load.sh '34' '100' '124') #[default]"
    set-option -g status-left "#[fg=colour239,bg=colour255]#{?client_prefix,#[reverse],} Session: #S #[default]"
}
